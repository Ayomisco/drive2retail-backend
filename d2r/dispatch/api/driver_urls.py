"""driver routes — Driver app — today's trip, arrive, complete, fail, return.

Endpoint contracts are specified in docs/backend/02-api.md.
"""

from django.urls import path  # noqa: F401

app_name = "driver"

urlpatterns: list = []
